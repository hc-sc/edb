module.exports = exports = function SubmissionPlugin(schema, options) {
  // schema.virtual('packagetype').get(function() {
  //   console.log('in here');
  //   return this.incremental ? 'Incremental' : 'Full';
  // });

  // schema.post('find', )
  console.log('submission plugin loaded');
};
