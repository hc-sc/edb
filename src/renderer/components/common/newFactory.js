//import {Substance} from "../../../models/substance.model";
export default class NewFactory {
    static getObject(objectName) {
        // if (objectName === "substance") {
        //     return new Substance();
        // } else {
        //     console.log(" something else");
        // }
    }
}